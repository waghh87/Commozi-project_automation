/**
 * Card Generator - Handles test card and test detail generation
 */
import type { TestResultData } from '../types';
/**
 * Generate a single test card
 */
export declare function generateTestCard(test: TestResultData): string;
/**
 * Generate test details section (history, steps, errors, AI suggestions)
 */
export declare function generateTestDetails(test: TestResultData, cardId: string): string;
/**
 * Generate grouped tests by file
 */
export declare function generateGroupedTests(results: TestResultData[]): string;
